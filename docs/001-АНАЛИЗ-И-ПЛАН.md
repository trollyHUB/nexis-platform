# 001 - АНАЛИЗ ПРОЕКТА И ДЕТАЛЬНЫЙ ПЛАН

**Дата создания:** 26.12.2025  
**Проект:** Identity & Social  
**Статус:** Начало разработки

---

## 📋 АНАЛИЗ ФАЙЛА "НАЧАЛО.md"

### ✅ ЧТО СДЕЛАНО ПРАВИЛЬНО:

1. **Выбор технологий:**
   - Kotlin + Spring Boot 3.x - современный стек
   - Gradle Kotlin DSL - типобезопасные скрипты сборки
   - Amazon Corretto 21 - стабильный LTS JDK
   - PostgreSQL 16 - мощная база данных

2. **Архитектура:**
   - Слоистая архитектура: Controller → Service → Repository → Entity
   - Правильное разделение ответственности
   - DTO для API (не раскрываем Entity напрямую)

3. **Инфраструктура:**
   - Docker для PostgreSQL - правильный подход
   - .env для секретов - безопасно
   - .gitignore настроен правильно

### ⚠️ ЧТО НУЖНО УЛУЧШИТЬ/ДОБАВИТЬ:

1. **JWT библиотека отсутствует в зависимостях:**
   - Нужно добавить `io.jsonwebtoken:jjwt-api`, `jjwt-impl`, `jjwt-jackson`

2. **Миграции базы данных:**
   - `ddl-auto=update` - только для разработки
   - Для продакшена нужен Flyway или Liquibase

3. **PostgreSQL переустановка:**
   - Использовать ТОЛЬКО Docker PostgreSQL
   - Не устанавливать PostgreSQL локально
   - Всё через `docker-compose up -d`

4. **Дополнительные библиотеки:**
   - MapStruct для маппинга Entity ↔ DTO
   - Spring Boot Actuator для мониторинга

---

## 🐳 POSTGRESQL ЧЕРЕЗ DOCKER (БЕЗ ЛОКАЛЬНОЙ УСТАНОВКИ)

Так как у тебя проблемы с локальным PostgreSQL, будем использовать ТОЛЬКО Docker.

### Шаги:
1. Убедись что Docker Desktop запущен
2. В терминале выполни: `docker-compose up -d`
3. PostgreSQL будет доступен на `localhost:5432`
4. Никакого локального PostgreSQL не нужно!

---

## 📦 ДЕТАЛЬНЫЙ ПЛАН РАЗРАБОТКИ

### ФАЗА 0: ПОДГОТОВКА ✅ ЗАВЕРШЕНО
- [x] Создать проект в IDEA
- [x] Настроить Gradle и зависимости
- [x] Добавить недостающие зависимости (JWT)
- [x] Создать docker-compose.yml
- [x] Запустить PostgreSQL через Docker
- [x] Проверить подключение к базе

### ФАЗА 1: БАЗА ДАННЫХ И МОДЕЛИ (СЛЕДУЮЩИЙ ЭТАП)
- [ ] Создать Entity: `User`
- [ ] Добавить поля: id, username, email, password, createdAt, updatedAt
- [ ] Создать Repository: `UserRepository`
- [ ] Добавить миграции Flyway

### ФАЗА 2: АУТЕНТИФИКАЦИЯ (3-5 дней)
- [ ] Настроить Spring Security
- [ ] Реализовать JWT генерацию и валидацию
- [ ] Создать сервисы:
  - `AuthService` - регистрация и логин
  - `UserService` - работа с пользователями
  - `JwtService` - работа с токенами
- [ ] Создать DTO:
  - `RegisterRequest`
  - `LoginRequest`
  - `AuthResponse`
- [ ] Создать контроллеры:
  - `AuthController` (/api/auth/*)
  - `UserController` (/api/users/*)

### ФАЗА 3: REFRESH ТОКЕНЫ (1-2 дня)
- [ ] Добавить Refresh Token механизм
- [ ] Сохранение токенов в базе
- [ ] Endpoint для обновления токенов

### ФАЗА 4: ПРОФИЛЬ ПОЛЬЗОВАТЕЛЯ (2-3 дня)
- [ ] Расширить модель User:
  - firstName, lastName
  - avatar (URL)
  - bio (описание)
  - birthDate
- [ ] API для редактирования профиля
- [ ] Загрузка аватара (позже через MinIO)

### ФАЗА 5: EMAIL ПОДТВЕРЖДЕНИЕ (2-3 дня)
- [ ] Интеграция с Email сервисом
- [ ] Генерация токена подтверждения
- [ ] Endpoint для подтверждения email
- [ ] Шаблоны писем

### ФАЗА 6: ВОССТАНОВЛЕНИЕ ПАРОЛЯ (1-2 дня)
- [ ] Forgot password endpoint
- [ ] Reset password endpoint
- [ ] Безопасное хранение токенов сброса

### ФАЗА 7: SOCIAL ФУНКЦИОНАЛ (7-14 дней)
- [ ] Модели: Post, Comment, Like, Follow
- [ ] Лента постов (Feed)
- [ ] Подписки и подписчики
- [ ] Лайки и комментарии

---

## 🔧 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

### Шаг 1: Добавить зависимости для JWT
В `build.gradle.kts` добавить:
```kotlin
implementation("io.jsonwebtoken:jjwt-api:0.12.3")
runtimeOnly("io.jsonwebtoken:jjwt-impl:0.12.3")
runtimeOnly("io.jsonwebtoken:jjwt-jackson:0.12.3")
```

### Шаг 2: Создать docker-compose.yml
Файл уже должен быть создан по инструкции в НАЧАЛО.md

### Шаг 3: Запустить PostgreSQL
```bash
docker-compose up -d
docker ps  # проверить что контейнер запущен
```

### Шаг 4: Проверить что приложение запускается
Запустить `IdentitySocialApplication.kt`

---

## 📁 СТРУКТУРА ПАПОК ПРОЕКТА

```
identity-social/
├── docs/                           # 📚 Документация (все MD файлы тут)
│   ├── 001-АНАЛИЗ-И-ПЛАН.md       # Этот файл
│   ├── 002-xxx.md                  # Следующий документ
│   └── ...
├── src/
│   ├── main/
│   │   ├── kotlin/com/tolegen/identitysocial/
│   │   │   ├── config/            # Конфигурация (Security, JWT)
│   │   │   ├── controller/        # REST контроллеры
│   │   │   ├── dto/               # Data Transfer Objects
│   │   │   ├── entity/            # JPA сущности
│   │   │   ├── repository/        # JPA репозитории
│   │   │   ├── security/          # JWT фильтры, провайдеры
│   │   │   ├── service/           # Бизнес-логика
│   │   │   └── IdentitySocialApplication.kt
│   │   └── resources/
│   │       ├── db/migration/      # Flyway миграции
│   │       └── application.properties
│   └── test/
├── docker-compose.yml
├── .env
├── .gitignore
└── build.gradle.kts
```

---

## ⏱️ ПРИМЕРНЫЕ СРОКИ

| Фаза | Описание | Время |
|------|----------|-------|
| 0 | Подготовка и настройка | 1 день |
| 1 | База данных и модели | 1-2 дня |
| 2 | Аутентификация (JWT) | 3-5 дней |
| 3 | Refresh токены | 1-2 дня |
| 4 | Профиль пользователя | 2-3 дня |
| 5 | Email подтверждение | 2-3 дня |
| 6 | Восстановление пароля | 1-2 дня |
| 7 | Social функционал | 7-14 дней |

**ИТОГО MVP Identity & Social: ~3-4 недели**

---

## 📝 СЛЕДУЮЩИЙ ДОКУМЕНТ

После завершения Фазы 0 будет создан:
- `002-НАСТРОЙКА-ПРОЕКТА.md` - детальные шаги настройки

---

*Этот документ будет обновляться по мере продвижения проекта.*

