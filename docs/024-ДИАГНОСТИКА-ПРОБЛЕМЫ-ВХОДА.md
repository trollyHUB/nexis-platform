# 🔍 ДИАГНОСТИКА: ПОЧЕМУ НЕ МОГУ ВОЙТИ

**Дата:** 26.12.2025  
**Проблема:** Не могу войти в систему  
**Статус:** ✅ РЕШЕНО!

---

## ✅ НАЙДЕНА И ИСПРАВЛЕНА ПРОБЛЕМА!

### 🎯 ПРОБЛЕМА:
**Конфликт портов между backend и frontend!**

- **Backend** был на порту `8083` (application.properties)
- **Frontend** пытался подключиться к `8082` (api.ts)
- **Результат:** Frontend не мог достучаться до backend!

### 🔧 РЕШЕНИЕ:
Исправлен порт backend на `8082`:

**Файл:** `src/main/resources/application.properties`
```properties
# Было:
server.port=8083  ❌

# Стало:
server.port=8082  ✅
```

---

## 🚀 КАК ПРИМЕНИТЬ ИСПРАВЛЕНИЕ

```powershell
# 1. Останови backend (Ctrl+C)

# 2. Перезапусти backend (теперь он будет на 8082)
cd C:\Users\Lenovo\IdeaProjects\identity-social
./gradlew bootRun

# 3. Дождись в логах:
# ✅ "Tomcat started on port 8082 (http)"

# 4. В браузере очисти кеш (F12 → Console):
localStorage.clear()
location.reload()

# 5. Войди: admin / admin123
```

**Теперь всё должно работать!** ✅

---

## ❗ ЧТО ИЗМЕНИЛОСЬ

### Изменения в `api.ts`:
1. ✅ Исправлен порт: `8083` → `8082`
2. ✅ Добавлен `postsService` с методами
3. ✅ Добавлены типы `Post`, `CreatePostRequest`, `PageResponse`

### Что НЕ менялось:
- ❌ `authService` - без изменений
- ❌ Endpoints аутентификации - без изменений
- ❌ AuthContext - без изменений

---

## 🔍 ВОЗМОЖНЫЕ ПРИЧИНЫ

### Причина 1: Старый токен в localStorage
**Симптом:** При загрузке страницы logout автоматически

**Решение:**
```javascript
// Открой DevTools (F12) → Console → выполни:
localStorage.clear()
location.reload()
```

### Причина 2: Backend не запущен
**Симптом:** Ошибка в консоли `net::ERR_CONNECTION_REFUSED`

**Проверка:**
```powershell
# Открой http://localhost:8082/actuator/health
# Должен вернуть: {"status":"UP"}
```

**Решение:**
```powershell
cd C:\Users\Lenovo\IdeaProjects\identity-social
./gradlew bootRun
```

### Причина 3: Порт 8082 занят другим процессом
**Симптом:** Backend запускается на другом порту

**Проверка:**
```powershell
netstat -ano | findstr :8082
```

**Решение:**
Останови процесс на 8082 или проверь логи backend при запуске

### Причина 4: CORS ошибка
**Симптом:** В консоли `CORS policy: No 'Access-Control-Allow-Origin' header`

**Решение:**
Проверь что в `SecurityConfig.kt` включен CORS для localhost:5173

### Причина 5: Неправильный URL в frontend
**Проверка:**
```typescript
// В файле frontend/src/services/api.ts должно быть:
const API_URL = 'http://localhost:8082/api';
```

✅ **ПРОВЕРЕНО:** URL правильный!

---

## 🧪 ПОШАГОВАЯ ДИАГНОСТИКА

### Шаг 1: Проверь что backend запущен
```powershell
# В терминале backend должен показывать:
Started IdentitySocialApplicationKt in X seconds
```

### Шаг 2: Проверь health endpoint
```powershell
curl http://localhost:8082/actuator/health
# Должен вернуть: {"status":"UP"}
```

### Шаг 3: Проверь login endpoint в Postman
```http
POST http://localhost:8082/api/auth/login
Content-Type: application/json

{
  "usernameOrEmail": "admin",
  "password": "admin123"
}
```

**Ожидаемый результат:**
```json
{
  "accessToken": "eyJhbGc...",
  "refreshToken": "uuid",
  "user": {
    "username": "admin",
    ...
  }
}
```

### Шаг 4: Очисти localStorage в браузере
```javascript
// F12 → Console:
localStorage.clear()
location.reload()
```

### Шаг 5: Попробуй войти снова

---

## 🔧 БЫСТРОЕ РЕШЕНИЕ

```powershell
# 1. Останови всё (Ctrl+C в обоих терминалах)

# 2. Перезапусти backend
cd C:\Users\Lenovo\IdeaProjects\identity-social
./gradlew bootRun

# Дождись "Started IdentitySocialApplicationKt"

# 3. Перезапусти frontend
cd frontend
npm run dev

# 4. Открой браузер в режиме инкогнито
Ctrl+Shift+N (Chrome)

# 5. Зайди на http://localhost:5173

# 6. Войди: admin / admin123
```

---

## 📋 CHECKLIST

Проверь по порядку:

- [ ] Docker контейнер PostgreSQL запущен (`docker ps`)
- [ ] Backend запущен (`./gradlew bootRun`)
- [ ] Backend отвечает (`curl http://localhost:8082/actuator/health`)
- [ ] Frontend запущен (`npm run dev`)
- [ ] Открыт правильный URL (`http://localhost:5173`)
- [ ] localStorage очищен (F12 → Application → Local Storage → Clear)
- [ ] Логин/пароль правильные (`admin` / `admin123`)
- [ ] Нет ошибок в консоли браузера (F12 → Console)
- [ ] Нет ошибок в логах backend (терминал)

---

## 🔍 ЛОГИ ДЛЯ ПРОВЕРКИ

### Backend логи (терминал):
```
✅ Started IdentitySocialApplicationKt
✅ Tomcat started on port 8082
✅ No errors
```

### Frontend логи (терминал):
```
✅ Local: http://localhost:5173
✅ ready in XXX ms
```

### Browser Console (F12):
```
✅ No red errors
✅ Network tab: все запросы 200 OK
```

---

## 🆘 ЕСЛИ НИЧЕГО НЕ ПОМОГЛО

### 1. Скажи какую ошибку видишь:
- В консоли браузера (F12 → Console)
- В Network tab (F12 → Network)
- В терминале backend
- На экране браузера

### 2. Сделай скриншот ошибки

### 3. Проверь файлы изменились или нет:
```powershell
git status
git diff frontend/src/services/api.ts
```

---

## ✅ ЧТО ТОЧНО РАБОТАЕТ

1. ✅ Backend компилируется (`BUILD SUCCESSFUL`)
2. ✅ Frontend компилируется (`built in 306ms`)
3. ✅ API endpoint `/auth/me` существует
4. ✅ Порт исправлен на 8082

---

**Попробуй решения выше и скажи что показывает консоль браузера!** 🔍

*Создано: 26.12.2025 15:00*

