# 008 - НАСТРОЙКА ФРОНТЕНДА

**Дата:** 26.12.2025  
**Статус:** ⏳ ТРЕБУЕТ УСТАНОВКИ NODE.JS

---

## 🔧 ЧТО НУЖНО УСТАНОВИТЬ

### 1. Node.js (ОБЯЗАТЕЛЬНО)

**Скачай и установи:** https://nodejs.org/

Рекомендую версию **LTS (22.x)** или **20.x**

После установки проверь в PowerShell:
```powershell
node --version   # Должно показать v22.x.x или v20.x.x
npm --version    # Должно показать 10.x.x
```

### 2. Postman (РЕКОМЕНДУЕТСЯ для тестирования API)

**Скачай:** https://www.postman.com/downloads/

Это программа для тестирования API запросов. Можно:
- Отправлять POST/GET запросы
- Сохранять коллекции запросов
- Видеть красивый JSON ответ

### 3. Git (если ещё нет)

**Скачай:** https://git-scm.com/download/win

Для версионирования кода и загрузки на GitHub.

---

## 📋 АЛЬТЕРНАТИВЫ POSTMAN

Если не хочешь Postman, можно использовать:

### Встроенный HTTP Client в IDEA

IntelliJ IDEA Ultimate имеет встроенный HTTP клиент:
1. Создай файл `test.http` в проекте
2. Пиши запросы прямо в IDE

Пример `test.http`:
```http
### Главная страница
GET http://localhost:8082/

### Регистрация
POST http://localhost:8082/api/auth/register
Content-Type: application/json

{
  "username": "testuser",
  "email": "test@example.com",
  "password": "Password123"
}

### Логин
POST http://localhost:8082/api/auth/login
Content-Type: application/json

{
  "usernameOrEmail": "testuser",
  "password": "Password123"
}
```

### curl (уже есть в Windows)

```powershell
curl http://localhost:8082/
```

### PowerShell Invoke-RestMethod

```powershell
Invoke-RestMethod -Uri "http://localhost:8082/" -Method Get
```

### Bruno (бесплатная альтернатива Postman)

**Скачай:** https://www.usebruno.com/

Открытый и бесплатный, без аккаунта.

### Insomnia

**Скачай:** https://insomnia.rest/download

Ещё одна альтернатива Postman.

---

## 🌐 БРАУЗЕРНЫЕ РАСШИРЕНИЯ

Для просмотра JSON в браузере:

### Chrome
- **JSON Viewer** - https://chrome.google.com/webstore/detail/json-viewer

### Firefox
- Встроенный JSON просмотрщик (уже есть)

### Edge
- **JSON Formatter** - в Microsoft Store

---

## 🚀 ПОСЛЕ УСТАНОВКИ NODE.JS

Когда установишь Node.js, я создам:

1. **React фронтенд** с:
   - Страница входа
   - Страница регистрации
   - Профиль пользователя
   - Красивый дизайн (Tailwind CSS)

2. Запустишь его командой:
   ```powershell
   cd frontend
   npm install
   npm run dev
   ```

3. Откроешь **http://localhost:5173** и увидишь красивый интерфейс!

---

## ❓ НУЖНЫ ЛИ ДОПОЛНИТЕЛЬНЫЕ САЙТЫ/URL?

**НЕТ, ничего дополнительного не нужно!**

Всё работает локально на твоём компьютере:

| Сервис | URL | Описание |
|--------|-----|----------|
| Backend API | http://localhost:8082 | Spring Boot сервер |
| Frontend | http://localhost:5173 | React приложение |
| PostgreSQL | localhost:5432 | База данных (Docker) |

Никакие внешние сервисы не требуются. Всё на localhost.

---

## 📝 ИНСТРУКЦИЯ

1. **Установи Node.js** с https://nodejs.org/
2. **Перезапусти PowerShell/Terminal** (чтобы подхватился PATH)
3. **Напиши мне** "Node.js установлен"
4. Я создам фронтенд!

---

*Документ создан: 26.12.2025*

