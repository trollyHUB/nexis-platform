# 003 - ПЕРВЫЙ ЗАПУСК ПРИЛОЖЕНИЯ

**Дата:** 26.12.2025  
**Статус:** ✅ УСПЕШНО ЗАПУЩЕНО

---

## ✅ СТАТУС ПОДГОТОВКИ

- [x] Проект создан в IDEA
- [x] Зависимости загружены (Spring Boot 4.0, JWT, PostgreSQL)
- [x] Docker Desktop запущен
- [x] PostgreSQL работает в Docker
- [x] База данных `identity_social_db` создана
- [x] Структура папок готова

---

## 🚀 ЗАПУСК ПРИЛОЖЕНИЯ

### Способ 1: Через IDEA (рекомендуется)

1. Открой файл `IdentitySocialApplication.kt`
2. Найди зелёную стрелку ▶️ слева от `fun main`
3. Нажми на неё → **Run 'IdentitySocialApplication'**

### Способ 2: Через терминал

```powershell
cd "C:\Users\Lenovo\IdeaProjects\identity-social"
.\gradlew bootRun
```

---

## 📋 ЧТО ОЖИДАТЬ В КОНСОЛИ

При успешном запуске увидишь:

```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/

...
Tomcat started on port 8080
Started IdentitySocialApplication in X.XXX seconds
```

---

## 🌐 ПРОВЕРКА В БРАУЗЕРЕ

Открой: **http://localhost:8081**

> ⚠️ **ВАЖНО:** Порт изменён на 8081 (так как 8080 был занят)

### Что увидишь:
- Белая страница с формой логина
- Заголовок: "Please sign in"
- Поля: Username и Password

**Это нормально!** Spring Security по умолчанию защищает все endpoints.

---

## 🔐 ВРЕМЕННЫЙ ЛОГИН (для теста)

При запуске Spring Security генерирует временный пароль.  
Посмотри в консоли строку:

```
Using generated security password: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

- **Username:** `user`
- **Password:** скопируй из консоли

После логина увидишь ошибку 404 (Whitelabel Error Page) - это нормально, у нас пока нет страниц.

---

## ⚠️ ВОЗМОЖНЫЕ ОШИБКИ

### Ошибка подключения к базе данных

```
Connection refused to host: localhost; port: 5432
```

**Решение:**
1. Проверь что Docker Desktop запущен
2. Выполни: `docker ps` - должен быть контейнер `identity-social-db`
3. Если нет, выполни: `docker-compose up -d`

### Порт 8080 занят

```
Port 8080 is already in use
```

**Решение:**
1. Найди процесс: `netstat -ano | findstr :8080`
2. Убей процесс: `taskkill /PID <номер> /F`
3. Или измени порт в `application.properties`: `server.port=8081`

---

## ✅ УСПЕШНЫЙ ЗАПУСК - ЧЕКЛИСТ

- [ ] Консоль показывает "Started IdentitySocialApplication"
- [ ] http://localhost:8080 открывается
- [ ] Видишь форму логина Spring Security
- [ ] Можешь залогиниться с временным паролем

---

## 📝 СЛЕДУЮЩИЙ ЭТАП

После успешного запуска переходим к:
- `004-СОЗДАНИЕ-USER-ENTITY.md` - создание модели пользователя

